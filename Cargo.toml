[package]
name = "strata"
version = "0.1.0"
edition = "2024"
rust-version = "1.98"
license = "MIT"
description = "Git repository archaeology - hotspots, bus factor and change coupling - with zero dependencies."
repository = "https://github.com/GammaSphere/zerodephack"

# The entire point of this project. Do not add anything below this line.
[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
